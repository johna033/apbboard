/**
 * Created by homer on 12.09.14.
 */

var app = angular.module("apbboardApp", ['ui.bootstrap']);

/*app.config(['$routeProvider',
    function ($routeProvider) {
        $routeProvider.
            when('/admin.html', {
                templateUrl: './templates/adminSignInView.html'
            }).
            otherwise({
                redirectTo: '/'
            });
    }]);*/
